process {
    withName: FETCH_SBML {
        publishDir = [
            path: { "${params.outdir}/sbml" },
            mode: params.publish_dir_mode,
            pattern: '*.sbml.gz'
        ]
    }
}
