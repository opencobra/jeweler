process {
    withName: MEMOTE_REPORT_SNAPSHOT {
        ext.args = '--solver hybrid'
        publishDir = [
            path: { "${params.outdir}/memote_report_snapshot" },
            mode: params.publish_dir_mode,
            pattern: '*.html'
        ]
    }
}
